namespace CurrencyAggregator.Domain.Constants
{
    public enum CurrencyAggregationGroupingPeriodicity
    {
        FiveMinutes = 5,
        FifthMinutes = 15,
        ThirtyMinutes = 30,
        OneHour = 60
    }
}
